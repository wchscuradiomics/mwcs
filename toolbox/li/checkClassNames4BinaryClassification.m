% labels = checkClassNames4BinaryClassification(classNames,labels) check the classnames of labels are classNames. The out
% labels belongs to classNames.
% 
%  classNames:[1 2] or [1 0]. The first class is the positive class. For example, classNames = [1 0], where 1 is positive class and 0 is 
%  negative class.
%  labels (arg in): a vector.
%